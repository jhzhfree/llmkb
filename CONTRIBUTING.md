# 贡献指南

## 分支建议
- `main`：稳定主线
- `develop`：日常集成
- `feature/*`：功能开发
- `fix/*`：缺陷修复

## 提交信息建议
- `feat:` 新功能
- `fix:` 缺陷修复
- `refactor:` 重构
- `docs:` 文档
- `test:` 测试
- `chore:` 工程维护

## 核心架构约束
1. Java 负责业务状态、治理、权限、版本、审计和统一 API。
2. Python 负责模型推理、知识抽取、检索编排、图算法和重排序。
3. MySQL 保存权威事实；向量、全文、图谱索引均视为可重建派生数据。
4. 不允许业务代码直接绑定具体模型厂商或向量数据库 SDK；应通过 Provider / Adapter 接口接入。
5. 知识发布必须保留证据链、版本与裁决记录。
